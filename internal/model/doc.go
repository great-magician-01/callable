// Package model defines the unified, provider-agnostic message model shared
// by all providers: messages and content parts, requests, responses and
// usage, streaming events, thinking configuration, structured-output formats
// and tool definitions. It is an internal implementation detail; the public
// API is re-exported by the root package callable.
package model
